# Data — only data/sample/ is committed (it is what CI reproduces results from)
data/raw/
data/interim/
data/processed/
data/tmp/
!data/sample/
!data/sample/**

# Secrets
.env
.env.*
!.env.example
kaggle.json
access_token

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
htmlcov/
.coverage

# Models / binaries
*.pkl
*.joblib

# OS
Thumbs.db
.DS_Store

# Personal working notes — Hinglish, deliberately not part of the submission.
# These are the learning/journal layer, not deliverables.
docs/tasks-samjhao.md
docs/worklog.md
docs/seekha.md

# T5.10. English, but explicitly "not submitted -- this one is for you" (tasks.md).
docs/interview_prep.md

# Claude Code local working directory (worktrees, session state)
.claude/

# T5.1 decision ledgers. A ledger is a record of a run, not a derived document:
# committing one would make every rerun a diff, and the property CI checks is the
# chain (tests/test_ledger.py), not any particular file.
data/ledger/

# T5.8 repro logs. A run, not a document -- see src/mandateguard/eval/repro.py.
data/repro/

# scripts/check_drift.py consumes this; it is a run, not a document.
repro-report.txt
